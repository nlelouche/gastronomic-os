# Machine Learning Models

Place your TFLite models here.

Required files for Food Recognition:
- `food_classifier.tflite`: The quantized TFLite model.
- `food_labels.txt`: The label map (one label per line).
